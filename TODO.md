# 待办清单（TODO）

> 记录**情绪明确要求修改**的事项，以及已知但暂缓的问题。
> 这里只放公开、脱敏的内容（本文件随仓库公开）；运维细节见本地文档（不入库）。
> 完成一项就把它移到文末「已完成」并注明日期与 commit。

---

## 🔴 待处理

（暂无）

---

## 🟡 已知问题 / 暂不处理

### C. 游客能看到哪些内容（2026-09-21 定：默认屏蔽 R18G）
- 游客开放后，匿名访客**默认套用 R18G 推荐默认屏蔽组**（`blockedTagsFor(null)`），
  但**普通 NSFW（裸露/性暗示）是可见的** —— 这是「只屏蔽 R18G」这个选择的必然结果。
- ⚠️ 副作用：本站此前**整站需登录**，开放游客后等于**把普通 NSFW 也对全网公开**。
  ✅ **情绪 2026-09-21 明确回复「上线吧不管了」→ 维持现状，此条不再是待决问题。**
  备查（将来若要收紧）：①给游客再套一层「安全模式」（额外屏蔽 `nsfw`/`nipples`/`pussy` 等）；
  ②只对游客开放非 R18 作品（需作品级 R18 标记，库里目前没这个字段，要先补）。
- 判定入口在 `src/lib/r18g-tags.ts` 的 `blockedTagsFor()` —— 三种情况（游客 / 开了开关 / 没开）
  都收口在这一个函数，改口径只改它一处。

### D. 踩坑留档：把带条件渲染的提示块搬进另一个条件块里 → 提示静默消失

- 删「昵称」行那个按钮时，我把「改名成功」提示（`nickMsg`）一起挪进了头像区的
  `{nickEditing && (…)}` 面板里。结果**保存成功后提示永远不显示** ——
  因为 `saveNickname()` 成功时先 `setNickEditing(false)` 收起面板，
  提示随即被一起卸载，根本没机会渲染。
- 原补丁把它放在信息表那一行的**条件之外**所以正常；我搬家时没注意到这个依赖。
- ✅ 修法：提示块必须留在 `nickEditing` 块**外面**（代码里已加注释说明原因）。
- **通则**：移动 JSX 片段前，先确认它依赖的 state 在同一时刻是否还会被渲染 ——
  「关闭编辑态 + 显示结果」这种组合，提示天生不能放在编辑态块里。
- 判据：`/tmp/shots/verify-nick-row.mjs` 里那条「保存成功提示」断言（改成边等边抓，
  因为提示会随 `router.refresh()` 重挂而消失，等固定时长再读会假红）。

### A. `/u/[handle]` 改名前后的旧昵称链接会失效

- 改名时作品作者名会级联到新昵称，因此**指向旧昵称的 `/u/<旧昵称>` 会 404**（属于预期行为）。
- 用户名句柄始终可用（`getUserByHandle` 先昵称、再用户名）。
- 若要支持旧昵称跳转，需要额外的「昵称历史」表 —— 目前认为不值得。

### B. 昵称重名只靠应用层保证

- `users.author_name` **没有唯一索引**（只有 `username` 有 `lower(username)` 唯一索引）。
- 当前靠 `updateAuthorName` 在 `BEGIN IMMEDIATE` 事务内查重来防并发重名。
- 更彻底的做法是加 `CREATE UNIQUE INDEX ... ON users(author_name COLLATE NOCASE)`，
  但**存量数据若已有重名会导致建索引失败**，需要先清洗，故暂缓。

### E. 踩坑留档：`position: sticky` 会浮在后续兄弟之上 → 新加的卡片被压住

- 给生图台右侧加「生图历史」卡时，结果卡原本是 `position: sticky; top: 76px`。
  实测滚动 300px 时结果卡占 `[76,617]`、历史卡占 `[360,667]` → **两卡重叠**，
  历史缩略图被压在结果图底下（1440x900 与 1280x800 都复现）。
- **原因**：sticky 元素脱离常规流后**会盖住后面的同级兄弟**；它的浮动范围只受
  **父容器**约束，跟兄弟元素没有任何关系 —— 所以纯 CSS 做不到「钉住但给兄弟让位」。
- ✅ 修法：去掉 sticky（`position: relative`）。右列现在 结果卡+历史卡 ≈ 850px、
  已接近视口高度，钉住的收益本就很小。
- **通则**：给一个 sticky 元素**后面**加内容前，先量两件事 —— ①滚动若干位置看是否重叠；
  ②父容器高度是否还够它浮动。要「钉住 + 下方还有内容」，正确做法是把**整列**
  做成 sticky + `max-height` + 内部滚动，而不是只钉其中一张卡。
- 判据：滚动 0/300/800/1500/3000px 五个位置逐点比较两卡的 `getBoundingClientRect()`。

### F. 踩坑留档：可删内容的图片不能用长 `max-age` 缓存

- 历史图原本返回 `Cache-Control: private, max-age=3600`。实测**删除记录后**，
  浏览器直接拿磁盘缓存回 `200`，把已删的图继续显示出来（服务端明明已经 404）——
  用户会以为「删不掉」。
- ✅ 修法：改 `private, no-cache` + `ETag`（内容不变走 `304`，删除立刻生效）。
- **通则**：**用户可删除**的资源不要用长 `max-age`；要么短缓存 + 校验，要么 `no-cache` + ETag。
  验证时不能用普通 `fetch` 下结论 —— 它会命中缓存，必须和 `cache: "no-store"` 对照，
  否则会把客户端缓存假象当成服务端 bug（这次就先误报了一次）。

---

## ✅ 已完成

| 日期 | 事项 | 备注 |
|---|---|---|
| 2026-09-21 | 生图台新增「生图历史」：结果卡下方展示最近 4 张缩略图，服务端保留 20 条（可展开/删除/清空/复用词） | commit `7ac8e18`；见下方「踩坑 E / F」 |
| 2026-09-21 | 个人资料页「昵称」行旁边的重复「修改」按钮已删除，改名入口只剩头像下「修改昵称」 | 见下方「踩坑 D」 |
| 2026-09-21 | 游客登录：画廊/详情/用户主页对游客开放只读；生图台/上传/头像跳登录；游客默认屏蔽 R18G | 情绪拍板「上线吧不管了」→ 维持现状，见「已知问题 C」 |
| 2026-09-19 | 个人资料页支持修改昵称（昵称 = 作品作者名，改名级联本人全部作品） | commit `f048ec7`；群友提供补丁，合并时修补三处安全缺陷（详见 `docs/DOCUMENTATION.md` §7.32） |
| 2026-09-19 | README 首行加致谢（里恩大人）+ `docs/assets/thanks-lien.jpg` | commit `f048ec7` |
| 2026-09-14 | 生图台接入 WeiLin 中文词库（11 分类 / 132 分组 / 4086 标签） | commit `0c09028` |
| 2026-09-14 | 提示词组「已选词气泡」+ 词库改为只读（移除用户编辑入口） | commit `1d31b83` |
