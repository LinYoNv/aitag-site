# 2026-09-02 工作记录

## 完成
1. 2 号机（154.12.28.103）部署完成：
   - Node v24.20.0 安装，git clone 代码，next build 成功
   - 部署目录 /root/aitag-deploy（standalone + static + public + data）
   - systemd 服务 aitag-site（active + enabled，开机自启）
   - 公网 http://154.12.28.103:3100 可访问
   - 数据迁移：10 条作品 + works 10 图 + uploads 4 图
2. 详情页 Grid 卡片布局（参照 aitag.win）：
   - 新建 src/components/CardMetaView.tsx
   - 重写 src/components/WorkDetailClient.tsx（Grid 3列→2列→1列，每卡图+参数一体）
   - globals.css 加 .detail-images/.img-card
   - 本机 dev (3200端口) 验证通过（CDP: 3卡片/3列/每图独立参数）
   - 测试数据：test-multi-001（3图合并作品）—— 本机 data/aitag.db
   - ⚠️ 此改动**尚未提交 git、未部署 2 号机**（用户先让处理别的问题）
3. Git 仓库：github.com/LinYoNv/aitag-site，commit 2ca6e58 已推送

## 进行中（用户报告）
- 用户和群友在 2 号机分别上传 2 张图，图片似乎没加载出来
- 用户休息中，要求：修改后提交 git → 2 号机拉取，避免修坏无法回档

## 待办
- [ ] 排查 2 号机上传图片不显示问题
- [ ] 确认 Grid 布局改动提交/部署时机

## 2026-09-02 续：上传图片 404 根因与修复
- 根因：Next.js standalone 静态服务启动时缓存 public 索引，运行期新写入 uploads 的文件 404
- 对照实验确认：服务启动前存在的图 200，启动后上传的图 404
- 应急修复：重启服务（已验证 2 张图恢复 200）—— 临时
- 根本修复（进行中）：上传改存 data/uploads/ + 新增 /api/images/[name] 动态返回，避免静态缓存
- 用户要求：改完提交 git → 2 号机 git pull 重部署

## 2026-09-02 完成：上传图片 404 已彻底修复并部署
- 修复方案：上传 API 改存 data/uploads/ + 新增 /api/images/[name] 动态服务（含 legacy public/images/uploads 回退）
- 已提交 git（cf9921d）并推送 GitHub，2 号机 pull + 重新 build + 重部署
- 2 号机验证：用户上传的 2 张图（黑雪日常喵/嘟嘟嘟）已恢复 200；新上传立即可见无需重启
- 数据：12 作品（10种子 + 2用户上传），works 10图 + legacy uploads 6图
- Grid 卡片布局（CardMetaView + WorkDetailClient 重写）也已包含在 cf9921d 一起部署
- ⚠️ 部署过程踩坑：恢复数据时 cp 嵌套造成 public/images/images/uploads，已修复
- 服务：systemd active + enabled，公网 200

## 待用户确认
- Grid 卡片布局效果（用户说"先不管"，但已随 cf9921d 部署，可在 2 号机 http://154.12.28.103:3100 查看多图作品效果）
- 旧上传图仍走 legacy 路径（重启后静态可用），新图走 /api/images

## 2026-09-02 续2：用户上传作品缺技术参数修复
- 根因：上传页组装 metadata 时把 comment（NovelAI完整参数JSON）嵌套存放，详情页只读顶层 → sampler/steps/seed 等不显示
- 修复：
  1. 代码（3bfd0cc）：upload/page.tsx 上传时平铺 comment 关键参数到顶层（sampler/steps/width/height/scale/seed/noise_schedule 等）
  2. 数据：2号机已修复 3 个已上传作品的 metadata（黑雪/嘟嘟嘟/空雨），平铺 comment
- 已推送 GitHub，2 号机 pull + build + 重部署，全部 200 验证通过
- 用户决定：中英文切换、月榜页 = 弃案（不需要）
- 部署踩坑记录：cp -r 到已存在目录会嵌套（images/images），恢复数据要用 cp 到具体文件/目录，别 cp -r 整个目录到已存在的父目录

## 2026-09-02 续3：登录验证系统（进行中）
- 需求（用户确认）：进网页必须先登录；admin 账号 admin/5sinibaba（管理员可删全部）；作者只能删自己的图；群友自助注册
- session 方式：Cookie + 服务端 session（SQLite）
- 已完成：
  - db.ts：加 users/sessions 表 + 用户/会话/删除方法
  - lib/auth.ts：scrypt 密码哈希、注册/登录/登出/currentUser、ensureAdmin
  - API：/api/register、/api/login、/api/logout、/api/me
- 进行中：middleware 登录拦截、登录/注册页面、上传绑定作者、详情页删除按钮 + DELETE API
- 待办：本机测试 → 提交 git → 2 号机部署
